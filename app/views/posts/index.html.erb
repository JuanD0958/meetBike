


  <div class="container">
    <div class="section">

      <div class="row">
        <div class="col l12 m12 s12 center">
          <h3><i class="mdi-content-send brown-text"></i></h3>
          <h4>Popular posts</h4>
          <ul class="collapsible" data-collapsible="accordion">
            <li>
        <div class="collapsible-header">Conoce rutas y personas</div>
        <div class="collapsible-body">
          <div class="container" align="center">
            <table>
              <thead>
                <tr>
                  <th>Ruta</th>
                  <th>Fecha</th>
                </tr>

              </thead>

              <tbody>
                <% @routes.each do |post| %>
                   <tr>
                      <td><%= link_to post.title, post %></td>
                      <td><%= post.created_at.strftime('%d/%b/%Y') %></td>
                    <tr>
                <% end %>
              </tbody>
            </table>
            
              <%= link_to 'Agregar tu Ruta', new_route_path, method: :patch,:class => "btn-large waves-effect waves-light teal lighten-1"%>
  
        </div>
        </div>
            </li>
            <li>
        <div class="collapsible-header">¿Interesado en comprar algo?</div>
        <div class="collapsible-body">
                      <div class="container" align="center">
            <table>
              <thead>
                <tr>
                  <th>Producto</th>
                  <th>Fecha</th>
                </tr>

              </thead>

              <tbody>
                <% @shops.each do |post| %>
                  <tr>
                      <td><%= link_to post.title, post %></td>
                      <td><%= post.created_at.strftime('%d/%b/%Y') %></td>

                      
                  </tr>
                <% end %>
              </tbody>
            </table>
            
              <%= link_to 'Agregar tu Producto', new_shop_path, method: :patch, :class => "btn-large waves-effect waves-light teal lighten-1"%>
         
        </div>


        </div>
            </li>
          </ul>
          
        </div>
      </div>
    </div>
  </div>

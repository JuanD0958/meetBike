
<div class="container">
    <div class="row">
        <div class="col s12 m12 l12">
          <div class="card light-blue darken-2">
            <div class="card-content white-text">
              <span class="card-title">  <%= @post.title %></span>
              <p> <%= @post.content %></p>
            </div>
                <div class="card-action"> <a href="https://www.google.com.co/maps/" target="_new">MAP</a>
            </div>
          </div>
        </div>
      </div>


<%if current_user.id.to_s == @post.usr  %>
  <%= link_to 'Editar publicación', edit_post_path(@post) %>
  <%= link_to 'Eliminar', { :action => :destroy, :id => @post.id }, :method => :delete, :data => { :confirm => 'Are you sure?' } %>
  
<%end%>
 
<%= link_to 'atras', posts_path %>
</div>